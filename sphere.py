import math
radius = float(input("enter the radius for the sphere ="))
surface_area = 4*math.pi*radius*radius
volume = (4/3)*math.pi*radius*radius*radius
print ("surface area =" + str(surface_area))
print ("volume=" + str(volume))